# SudoHub / СудоХаб

## Зависимости
- pip install socket
- pip install torch
- pip install transformers
- pip install math
- pip install sqlite3

    ### Модель подсчёта перплексности
        Модель не поставляется вместе с программой, из-за большого объёма ~ 500 Мб, её следует загрузить отдельно. Основной файл main сам догрузит, если будет присутствовать подключение к интернету. 
